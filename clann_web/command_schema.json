{
  "_note": "Typed option schema for the web client's dynamic forms (Step 2.2). Each option: name, type (enum|int|float|text|bool), default, help, and (for enum) values. Option names must match what the C command actually parses (verified against heuristic_search / set_parameters / reconstruct / nj / showtrees). Notably `hs` does NOT parse criterion= or seed= (they are set via the `set` command), so they are deliberately absent from the hs form. htmlview/open/resultjson are handled by the server and omitted.",

  "exe": {
    "summary": "Load source-tree files (options control name parsing and multicopy-tree handling at load time).",
    "options": [
      {"name": "maxnamelen", "type": "text", "default": "",
       "help": "Max taxon-name length before delimiter mode kicks in, or 'full' to keep whole names (disables delimiter mode)."},
      {"name": "delimiter_char", "type": "text", "default": "",
       "help": "Character that separates gene id from species name in tip labels (delimiter mode)."},
      {"name": "summary", "type": "enum", "default": "short", "values": ["short", "full"],
       "help": "Amount of load-time summary printed."},
      {"name": "autoprunemono", "type": "enum", "default": "no", "values": ["no", "yes"],
       "help": "Prune monophyletic same-species clades from multicopy trees at load; trees that become single-copy join the supertree pool."},
      {"name": "autodecompose", "type": "enum", "default": "no", "values": ["no", "yes"],
       "help": "Decompose multi-copy gene-family trees into ortholog subtree fragments at load (pristine trees kept for reconstruct)."},
      {"name": "autoweight", "type": "enum", "default": "", "values": ["clan", "splitviol", "bootstrap"],
       "help": "Automatically weight source trees by the chosen scheme."}
    ]
  },

  "set": {
    "summary": "Set global options that persist for the session.",
    "options": [
      {"name": "criterion", "type": "enum", "default": "dfit",
       "values": ["dfit", "sfit", "qfit", "recon", "rf", "ml"],
       "help": "Optimality criterion used by hs/nj/alltrees. recon = gene-tree reconciliation."},
      {"name": "seed", "type": "int", "default": "",
       "help": "Random-number seed. Set this before hs for reproducible searches."}
    ]
  },

  "hs": {
    "summary": "Heuristic search for the best supertree.",
    "options": [
      {"name": "nreps", "type": "int", "default": "10",
       "help": "Independent search replicates."},
      {"name": "nbest", "type": "int", "default": "",
       "help": "How many equally-best trees to retain."},
      {"name": "nthreads", "type": "int", "default": "",
       "help": "OpenMP threads (one replicate per thread). Default: all CPUs."},
      {"name": "swap", "type": "enum", "default": "tbr",
       "values": ["nni", "spr", "tbr"],
       "help": "Rearrangement operator (ignored when vns=yes)."},
      {"name": "start", "type": "enum", "default": "nj",
       "values": ["nj", "random", "memory"],
       "help": "Starting tree: NJ, random, or the tree currently in memory."},
      {"name": "nsteps", "type": "int", "default": "",
       "help": "Improvement steps per replicate before stopping."},
      {"name": "maxswaps", "type": "int", "default": "",
       "help": "Max branch swaps per replicate."},
      {"name": "vns", "type": "enum", "default": "yes", "values": ["yes", "no"],
       "help": "Variable Neighborhood Descent (NNI→SPR→TBR escalation). The reliable default."},
      {"name": "ils", "type": "int", "default": "10",
       "help": "Iterated Local Search kicks without improvement before a replicate stops (0 = off)."},
      {"name": "strategy", "type": "enum", "default": "first", "values": ["first", "best"],
       "help": "Take the first improving move, or the best in the neighbourhood."},
      {"name": "lossmodel", "type": "enum", "default": "standard", "values": ["standard", "legacy"],
       "help": "recon only: duplication-loss model. standard = textbook LCA-mapping. legacy is deprecated and now runs standard."},
      {"name": "numspeciesrootings", "type": "text", "default": "mindup",
       "help": "recon only: mindup | all | <n> random rootings of the species tree."},
      {"name": "numgenerootings", "type": "text", "default": "mindup",
       "help": "recon only: mindup | all | <n> random rootings of each gene tree."},
      {"name": "duplications", "type": "float", "default": "1.0",
       "help": "recon only: weight per duplication."},
      {"name": "losses", "type": "float", "default": "1.0",
       "help": "recon only: weight per loss."}
    ]
  },

  "nj": {
    "summary": "Neighbour-joining supertree from average-consensus distances.",
    "options": [
      {"name": "missing", "type": "enum", "default": "4point",
       "values": ["4point", "ultrametric"],
       "help": "Estimate distances for taxon pairs not co-occurring in any source tree."},
      {"name": "savetrees", "type": "text", "default": "",
       "help": "Output file for the neighbour-joining supertree."}
    ]
  },

  "reconstruct": {
    "summary": "Reconcile every gene tree onto a species tree (duplications & losses).",
    "options": [
      {"name": "speciestree", "type": "enum", "default": "memory",
       "values": ["memory", "first"],
       "help": "Species tree to reconcile against: the tree in memory (from hs/nj) or the first source tree."},
      {"name": "showrecon", "type": "enum", "default": "no", "values": ["no", "yes"],
       "help": "Print detailed per-node reconciliation information."},
      {"name": "lossmodel", "type": "enum", "default": "standard", "values": ["standard", "legacy"],
       "help": "Duplication-loss model. standard is the only supported model; legacy is deprecated and now runs standard."},
      {"name": "dups", "type": "float", "default": "1.0",
       "help": "Weight per duplication."},
      {"name": "losses", "type": "float", "default": "1.0",
       "help": "Weight per loss."}
    ]
  },

  "showtrees": {
    "summary": "Display the loaded input gene trees.",
    "options": [
      {"name": "display", "type": "enum", "default": "no", "values": ["yes", "no"],
       "help": "Print the trees as ASCII to the log. Off by default in the web app — the viewer shows them regardless, and ASCII is slow for large sets."},
      {"name": "namecontains", "type": "text", "default": "",
       "help": "Only trees whose name contains this string."},
      {"name": "range", "type": "text", "default": "",
       "help": "Tree-number range, e.g. 1-5."}
    ]
  },

  "usertrees": {
    "summary": "Score/test user-supplied candidate supertrees against the loaded gene trees. Set the criterion first with the selector above (dfit, sfit, qfit, rf, or ml — not recon/avcon/mrp).",
    "options": [
      {"name": "file", "type": "file", "positional": true, "required": true, "default": "",
       "help": "File of candidate supertree topologies to score (upload it first, then choose it here)."},
      {"name": "tests", "type": "enum", "default": "no", "values": ["no", "yes"],
       "help": "Run ML-based topology tests (KH/SH/AU) comparing the candidates. Only meaningful under criterion=ml."},
      {"name": "nboot", "type": "int", "default": "1000",
       "help": "Bootstrap replicates for the topology tests (tests=yes)."},
      {"name": "testsfile", "type": "text", "default": "mltest_results.txt",
       "help": "Output filename for the topology-test results."},
      {"name": "printsourcescores", "type": "enum", "default": "no", "values": ["no", "yes"],
       "help": "Write each candidate's per-source-tree scores to sourcetree_scores.txt."},
      {"name": "savetrees", "type": "text", "default": "",
       "help": "Output file to save the scored candidate trees."},
      {"name": "nthreads", "type": "int", "default": "",
       "help": "OpenMP threads for the topology tests. Default: all CPUs."}
    ]
  },

  "consensus": {
    "summary": "Build a consensus tree from the trees in memory (loaded source trees, a supertree search, or a bootstrap run).",
    "options": [
      {"name": "data", "type": "enum", "default": "source", "values": ["source", "supertrees", "bootstraps"],
       "help": "Which trees to summarise: source = the loaded (universally distributed) gene trees; supertrees = results of a prior hs search in memory; bootstraps = trees from a prior bootstrap run."},
      {"name": "method", "type": "enum", "default": "minor", "values": ["minor", "majrule", "strict"],
       "help": "Consensus type: strict = splits present in every tree; majrule = splits in >50%; minor = majority-rule plus all compatible minor components. For a custom cut-off (0.5-1.0) use the extra-options box."},
      {"name": "filename", "type": "text", "default": "consensus.ph",
       "help": "Output file for the consensus tree."},
      {"name": "guidetree", "type": "file", "default": "",
       "help": "Optional guide-tree file used to order the consensus (upload it first, then choose it here)."}
    ]
  },

  "bootstrap": {
    "summary": "Bootstrap supertree analysis: resample the source trees and repeat the search under the selected criterion.",
    "options": [
      {"name": "nreps", "type": "int", "default": "100",
       "help": "Number of bootstrap replicates."},
      {"name": "swap", "type": "enum", "default": "tbr", "values": ["tbr", "spr", "nni"],
       "help": "Branch-swapping operator used in each replicate's search."},
      {"name": "consensus", "type": "enum", "default": "majrule", "values": ["majrule", "strict", "minor"],
       "help": "Consensus type summarising the bootstrap replicate trees."},
      {"name": "missing", "type": "enum", "default": "4point", "values": ["4point", "ultrametric"],
       "help": "How to estimate distances for taxa missing from a source tree."},
      {"name": "consensusfile", "type": "text", "default": "",
       "help": "Output file for the bootstrap consensus tree."},
      {"name": "nthreads", "type": "int", "default": "",
       "help": "OpenMP threads. Default: all CPUs."}
    ]
  },

  "alltrees": {
    "summary": "Exhaustively score every possible supertree under the selected criterion (feasible only for few taxa).",
    "options": [
      {"name": "nbest", "type": "int", "default": "",
       "help": "How many equally-best trees to retain."},
      {"name": "keep", "type": "int", "default": "",
       "help": "Keep every tree scoring within this many steps of the best."},
      {"name": "range", "type": "text", "default": "",
       "help": "Only evaluate this range of tree numbers, e.g. 1-1000."},
      {"name": "savetrees", "type": "text", "default": "",
       "help": "Output file to save the evaluated trees."}
    ]
  },

  "recluster": {
    "summary": "Cluster a landscape TSV (written by hs visitedtrees=) into groups of similar supertrees at an RF threshold.",
    "options": [
      {"name": "file", "type": "file", "positional": true, "required": true, "default": "",
       "help": "Landscape TSV file from a prior 'hs visitedtrees=' run (upload it first, then choose it here)."},
      {"name": "clusterthreshold", "type": "float", "default": "0.2",
       "help": "RF-distance threshold below which trees are placed in the same cluster."},
      {"name": "clusterorderby", "type": "enum", "default": "score", "values": ["score", "visits"],
       "help": "Order the reported clusters by best score or by number of visits."},
      {"name": "clusteroutput", "type": "text", "default": "treeclusters.tsv",
       "help": "Output file for the cluster assignments."}
    ]
  },

  "savetrees": {
    "summary": "Save source trees to a Phylip file. Subsets can be selected by number range or name.",
    "options": [
      {"name": "filename", "type": "text", "default": "",
       "help": "Output file (Phylip format)."},
      {"name": "range", "type": "text", "default": "",
       "help": "Only save this range of tree numbers, e.g. 1-5."},
      {"name": "namecontains", "type": "text", "default": "",
       "help": "Only save trees whose name contains this string."},
      {"name": "fullnames", "type": "bool", "default": "",
       "help": "Write full gene-copy tip names rather than collapsed species names."}
    ]
  },

  "excludetrees": {
    "summary": "Exclude source trees from subsequent analyses, selected by a variety of criteria.",
    "options": [
      {"name": "range", "type": "text", "default": "",
       "help": "Exclude trees in this number range, e.g. 1-5."},
      {"name": "size", "type": "int", "default": "",
       "help": "Select trees by number of taxa."},
      {"name": "namecontains", "type": "text", "default": "",
       "help": "Exclude trees whose name contains this string."},
      {"name": "containstaxa", "type": "text", "default": "",
       "help": "Exclude trees that contain these taxa (comma-separated)."},
      {"name": "score", "type": "text", "default": "",
       "help": "Exclude trees selected by score."},
      {"name": "singlecopy", "type": "bool", "default": "",
       "help": "Exclude single-copy trees."},
      {"name": "multicopy", "type": "bool", "default": "",
       "help": "Exclude multicopy trees."}
    ]
  },

  "includetrees": {
    "summary": "Restore previously excluded source trees, selected by a variety of criteria.",
    "options": [
      {"name": "range", "type": "text", "default": "",
       "help": "Restore trees in this number range, e.g. 1-5."},
      {"name": "size", "type": "int", "default": "",
       "help": "Select trees by number of taxa."},
      {"name": "namecontains", "type": "text", "default": "",
       "help": "Restore trees whose name contains this string."},
      {"name": "containstaxa", "type": "text", "default": "",
       "help": "Restore trees that contain these taxa (comma-separated)."},
      {"name": "score", "type": "text", "default": "",
       "help": "Restore trees selected by score."}
    ]
  },

  "deletetaxa": {
    "summary": "Delete taxa from every source tree in memory (pruned, preserving branch lengths). Use restoretaxa to undo.",
    "options": [
      {"name": "taxa", "type": "text", "positional": true, "required": true, "default": "",
       "help": "Taxa to delete from all trees, space-separated (e.g. Human Chimp)."},
      {"name": "mintaxa", "type": "int", "default": "",
       "help": "Drop any tree left with fewer than this many taxa after deletion."}
    ]
  },

  "rfdists": {
    "summary": "Compute Robinson-Foulds distances between all pairs of source trees.",
    "options": [
      {"name": "output", "type": "enum", "default": "matrix", "values": ["matrix", "vector"],
       "help": "Report the distances as a full matrix or as a vector (pairwise list)."},
      {"name": "missing", "type": "enum", "default": "4point", "values": ["4point", "ultrametric", "none"],
       "help": "How to handle taxa missing from a tree when comparing."},
      {"name": "filename", "type": "text", "default": "",
       "help": "Output file for the distances."}
    ]
  },

  "sprdists": {
    "summary": "Estimate SPR distances of the real data versus ideal and randomised versions.",
    "options": [
      {"name": "supertree", "type": "enum", "default": "create", "values": ["create", "memory"],
       "help": "Reference supertree: build one (nj) or use the tree currently in memory."},
      {"name": "dorandomisation", "type": "enum", "default": "no", "values": ["no", "yes"],
       "help": "Also compute SPR distances for randomised versions of the data (slower)."},
      {"name": "outfile", "type": "text", "default": "",
       "help": "Output file for the SPR distance results."}
    ]
  },

  "yaptp": {
    "summary": "'Yet another PTP' — a permutation-tail-probability randomisation test of phylogenetic signal.",
    "options": [
      {"name": "nreps", "type": "int", "default": "100",
       "help": "Number of permutation replicates."},
      {"name": "method", "type": "enum", "default": "equiprobable", "values": ["equiprobable", "markovian"],
       "help": "Tree-randomisation model used to build the null distribution."}
    ]
  },

  "generatetrees": {
    "summary": "Generate random supertrees and score them against the source trees in memory (builds a null score distribution).",
    "options": [
      {"name": "ntrees", "type": "int", "default": "100",
       "help": "Number of random supertrees to generate (or 'all' via the extra-options box)."},
      {"name": "method", "type": "enum", "default": "equiprobable", "values": ["equiprobable", "markovian"],
       "help": "Random-tree generation model."},
      {"name": "sourcedata", "type": "enum", "default": "real", "values": ["real", "randomised", "ideal"],
       "help": "Which source-tree data to score the generated trees against."},
      {"name": "nbins", "type": "int", "default": "20",
       "help": "Number of bins in the score histogram."},
      {"name": "savescores", "type": "enum", "default": "no", "values": ["no", "yes"],
       "help": "Write all generated-tree scores to file."}
    ]
  }
}
