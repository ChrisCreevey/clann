# Build configuration. Project metadata lives in setup.cfg so that older
# setuptools (which predates PEP 621 [project] support) installs correctly too.
[build-system]
requires = ["setuptools>=45", "wheel"]
build-backend = "setuptools.build_meta"
